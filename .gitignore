# Staged / unpublished writing - kept ready at its final path but NEVER public.
# GitHub Pages serves any committed file, so this draft is ignored on purpose.
# To publish: remove the line below, add a card in /writing/index.html, commit + push.
/writing/the-attribution-problem-is-unsolved/
